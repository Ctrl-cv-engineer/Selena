请为下面这个能力生成 {target_count} 条不重复的中文用户输入示例。

目标：
- 这些示例会写入意图识别向量数据库
- 用来帮助系统在启动前补齐该能力的可能触发说法

要求：
- 只保留真实用户可能说出的输入
- 不要重复已有示例
- 不要复述内部字段名
- 不要输出任何解释
- 直接返回 JSON，格式为 {{"examples": ["示例1", "示例2"]}}

请重点覆盖：
- 直接请求
- 口语化请求
- 含糊但仍然能明显触发该能力的请求
- 不同措辞但相同意图的自然表达

能力信息如下：
{ability_context_json}
