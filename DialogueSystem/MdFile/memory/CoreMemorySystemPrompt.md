【关键记忆】
以下是始终可见的关键记忆层。它分为两层：
- `持久核心记忆`：跨程序重启保留，只放稳定偏好、长期计划、长期身份信息与跨会话开放循环。
- `当前话题工作层`：只服务当前 topic，用来表示当前进展、当前环境与当前关系信号，不默认跨下次启动保留。

使用原则：
- 优先关注“持久核心记忆”中的用户画像和开放循环。
- “当前话题工作层”只表示当前阶段，不是历史档案。
- 如果这些信息与当前轮对话明确冲突，以当前轮对话为准。

{{MEMORY_BODY}}
