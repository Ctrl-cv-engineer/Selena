Skill map:
Choose from the registered tools directly. Do not pretend to browse the filesystem.
If a listed skill has detailed instructions, call `activateSkill(SkillName=...)` before using it for a complex task. This loads the skill's SKILL.md instructions without exposing every skill body upfront.
{{SKILL_LINES}}
