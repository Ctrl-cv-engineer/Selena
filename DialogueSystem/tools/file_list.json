{
  "type": "function",
  "function": {
    "name": "listLocalDirectory",
    "description": "List files and folders inside an allowed local workspace directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "Path": {
          "type": "string",
          "description": "Absolute or workspace-relative path to a directory."
        },
        "Limit": {
          "type": "integer",
          "description": "Optional maximum number of entries to return."
        }
      },
      "required": ["Path"],
      "additionalProperties": false
    }
  },
  "x-dialogue-metadata": {
    "toolset": "file_read",
    "risk_level": "guarded",
    "admin_only": false,
    "requires_approval": false,
    "backend": "direct",
    "supports_checkpoint": false,
    "supports_redaction": true
  }
}
